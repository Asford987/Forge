# Forge
Cargo like C++ package manager

Allows seamsless integration with other languages (Rust, Python, Go, Zig, ...)

- Forge install
- Forge publish
- Forge build (--arch)
- Forge run
- Forge new (--bin --lib)
